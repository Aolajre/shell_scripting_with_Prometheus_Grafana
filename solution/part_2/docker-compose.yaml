version: '3.8'

services:
  assignment-webserver:
    image: f6c6c5905cbe
    container_name: assignment-webserver
    ports:
      - 9090:8080
    environment:
      - BORDER_COLOR=blue
    volumes:
      - ./data.csv:/app/data.csv